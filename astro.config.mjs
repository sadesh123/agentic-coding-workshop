import { defineConfig } from 'astro/config';

// 🥚 KIRO TIP: Use Kiro to add new Astro integrations like @astrojs/react or @astrojs/sitemap by asking it to update this config file.

export default defineConfig({
  output: 'static',
});
